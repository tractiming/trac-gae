# The app's runtime configuration.

application: trac-us
version: 1
runtime: python27
api_version: 1
threadsafe: yes

libraries:
- name: django
  version: "1.5"

- name: MySQLdb
  version: "latest"

#env_variables:
#  DJANGO_SETTINGS_MODULE: 'settings.common'

#builtins:
#- django_wsgi: on


handlers:
- url: /static
  static_dir: static
  secure: always

#- url: /oauth2
#  script: main.application
#  secure: always

- url: /.*
  script: main.application
  secure: always
#  login: required

